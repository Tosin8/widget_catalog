
// Theme - 1. Descendant widgets obtain the current theme's ThemeData object using Theme.of 

// When a widget uses Theme.of, it is automaticaly rebuilt if the theme later changes, so that the changes can be applied. 

// ThemeData - describes the actual configuration of a theme. 
// AnimatedTheme - animates the ThemeData when it changes rather than changing the theme all at once. 

// MaterialApp - which includes an AnimatedTheme widget configured via the MaterialApp.theme argument. 